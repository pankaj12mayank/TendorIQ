# Dev / CI / local venv — includes runtime deps from requirements.txt
# Install: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
mypy>=1.14.0
pre-commit>=4.0.0
ruff>=0.9.0
