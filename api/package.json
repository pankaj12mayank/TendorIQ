{
  "name": "@tendoriq/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn src.main:app --reload --port 8000",
    "start": "uvicorn src.main:app --port 8000",
    "lint": "ruff check src/",
    "format": "ruff format src/",
    "typecheck": "mypy src/",
    "test": "pytest",
    "test:unit": "pytest tests/unit tests/integration -q",
    "db:migrate": "alembic upgrade head"
  },
  "devDependencies": {}
}
