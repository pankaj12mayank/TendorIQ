/** Minimal motion tokens (Framer Motion optional). */

export const staggerContainer = {
  hidden: { opacity: 0 },
  show: { opacity: 1, transition: { staggerChildren: 0.05 } },
};

export const sidebarLayoutTransition = { duration: 0.2 };
